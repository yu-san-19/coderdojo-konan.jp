- provide :title, '記事一覧'

%h1 Listing posts

%table
  %thead
    %tr
      %th Body
      %th Title
      %th Published at
      %th Author
      %th Slug

  %tbody
    - @posts.each do |post|
      %tr
        %td= post.body
        %td= post.title
        %td= post.published_at
        %td= post.author_id
        %td= post.slug

%br
